package graphics;

import javax.swing.JButton;





//This is just a test class to see if the graphics work
public class MyGraphic{
	
	public static void main(String[] args)
	{
		/*
		//Create a frame
		MyFrame mf = new MyFrame();
		//Create a panel
		MyPanel mp = new MyPanel();
		//Add panel to frame
		mf.add(mp);
		//Set the panel's size
		mp.setSize(500, 500);
		//Create a button
		JButton jb = new JButton();
		//Add button to MyPanel
		mp.add(jb);
		//Set size of the button
		jb.setSize(50, 50);
		//Set the location of the button
		jb.setLocation(50, 0);
		//Create listener for the button
		MyButtonListener listener = new MyButtonListener(mp);
		//Add listener to the button
		jb.addActionListener(listener);
		
		//make the frame visible
		mf.setVisible(true);
		*/
	}

}
